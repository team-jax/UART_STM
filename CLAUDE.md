# STM32 로봇 손 — 작업 지시서 (순서대로 하나씩 할 것)

> 2026-07-13 기준. 수정된 `main.c` 기준으로 작성.
> 각 단계는 **위에서부터 순서대로** 진행. 체크박스에 완료 표시하며 진행할 것.

---

## STEP 1. 수정된 main.c 프로젝트에 반영

- [ ] 다운로드한 `main.c`를 프로젝트의 `Core/Src/main.c` 자리에 **덮어쓰기**
- [ ] CubeIDE(또는 사용 중인 빌드 환경)에서 **Clean → Build**
- [ ] 빌드 에러 0개 확인

> ⚠ 빌드 에러가 나면: 에러 메시지 전체를 복사해서 나(Claude)한테 줄 것. 임의로 고치지 말 것.

---

## STEP 2. 하드웨어 안전 확인 (보드 켜기 전)

- [ ] 서보 전원은 **외부 5~6V**에서 공급되는지 확인 (MCU 보드 5V 핀 ❌ — 서보 11개 스톨 시 수 A 소모)
- [ ] 외부 전원 GND와 STM32 보드 GND가 **서로 연결**되어 있는지 확인 (GND 공통 안 되면 PWM 신호 인식 안 됨)
- [ ] **손가락 사이에 물체 없는 상태**로 준비
  - 이유: A/B 서보가 미러 조립인지 아직 모름. 미러인데 같은 각도를 주면 서로 반대로 밀어서 모터가 스톨(과전류) 됨

---

## STEP 3. 펌웨어 굽고 UART 연결

- [ ] 펌웨어 플래시 (ST-Link)
- [ ] USB-UART 어댑터 연결: 어댑터 **RX → PA9(TX)**, 어댑터 **TX → PA10(RX)**, GND 공통
- [ ] 터미널 프로그램(RealTerm, Hercules 등) 설정: **115200 baud, 8-N-1**, HEX 송신 모드

---

## STEP 4. 제스처 명령 테스트 (핵심)

터미널에서 아래 HEX 바이트를 그대로 송신:

| 하고 싶은 것 | 보낼 바이트 (HEX) | 기대 동작 |
|---|---|---|
| **손 쥐기 (GRIP)** | `AA F0 00 01 F1` | 손가락 서보 6개가 30° 위치로 이동 |
| **손 펴기 (OPEN)** | `AA F0 00 00 F0` | 손가락 서보 6개가 180° 위치로 이동 |

- [ ] GRIP 송신 → 6개 서보 모두 움직임 확인
- [ ] OPEN 송신 → 6개 서보 모두 반대로 움직임 확인
- [ ] GRIP↔OPEN 5회 반복해도 정상 동작 확인

### 결과에 따라 할 일

| 관찰 결과 | 판단 | 할 일 |
|---|---|---|
| 손가락들이 전부 **같은 방향**으로 움직임 | A/B 미러 조립 아님 | STEP 5로 진행 |
| A와 B가 **서로 반대**로 움직여서 손 모양이 안 됨 | A/B 미러 조립 | **STEP 4-1 수행** |
| 일부 서보만 움직임 | 배선 or 채널 문제 | 안 움직이는 서보의 핀 배선 확인 (아래 핀맵 참조), 그래도 안 되면 나한테 보고 |
| 아무것도 안 움직임 | UART or 전원 문제 | STEP 4-2 수행 |

**핀맵 (배선 확인용)**
```
FINGER1_A = PA6 (TIM3_CH1)   FINGER1_B = PA7 (TIM3_CH2)
FINGER2_A = PB0 (TIM3_CH3)   FINGER2_B = PB1 (TIM3_CH4)
FINGER3_A = PB6 (TIM4_CH1)   FINGER3_B = PB7 (TIM4_CH2)
```

### STEP 4-1. 미러 조립일 때 코드 수정법

`main.c`의 `PWM_Control_Task` 안 제스처 분기에서 **B채널 3줄만** 아래처럼 변경:

```c
uint32_t g  = Angle_to_CCR(a);
uint32_t gm = Angle_to_CCR(180.0f - a);   /* ← 이 줄 추가 (미러 반전값) */

__HAL_TIM_SET_COMPARE(&htim3, TIM_CHANNEL_1, g);   /* FINGER1_A : 그대로 */
__HAL_TIM_SET_COMPARE(&htim3, TIM_CHANNEL_2, gm);  /* FINGER1_B : g → gm */
__HAL_TIM_SET_COMPARE(&htim3, TIM_CHANNEL_3, g);   /* FINGER2_A : 그대로 */
__HAL_TIM_SET_COMPARE(&htim3, TIM_CHANNEL_4, gm);  /* FINGER2_B : g → gm */
__HAL_TIM_SET_COMPARE(&htim4, TIM_CHANNEL_1, g);   /* FINGER3_A : 그대로 */
__HAL_TIM_SET_COMPARE(&htim4, TIM_CHANNEL_2, gm);  /* FINGER3_B : g → gm */
```

수정 후 다시 빌드 → 플래시 → STEP 4 재시도.

### STEP 4-2. 아무것도 안 움직일 때 점검 순서

1. [ ] 터미널이 **HEX 모드**로 보내는지 확인 (문자열 "AA F0..."을 텍스트로 보내면 안 됨)
2. [ ] TX/RX 교차 확인 (어댑터 TX ↔ 보드 PA10)
3. [ ] 서보 외부 전원 켜져 있는지, GND 공통인지 재확인
4. [ ] 개별 명령으로 서보 1개만 테스트: `AA 04 03 84 8B` (ID4를 90°로) → 이건 되는데 제스처만 안 되면 나한테 보고
5. [ ] 그래도 안 되면: 배선 사진 + 터미널 설정 스크린샷 들고 나한테 올 것

---

## STEP 5. 기존 기능 회귀 테스트

제스처 추가로 기존 개별 서보 제어가 깨지지 않았는지 확인:

| 보낼 바이트 (HEX) | 의미 | 기대 동작 |
|---|---|---|
| `AA 04 03 84 8B` | ID4(FINGER1_A)를 90.0° | 해당 서보만 90° 이동 |
| `AA 00 03 84 87` | ID0(어깨회전)을 90.0° | 어깨 서보만 90° 이동 |
| `AA F0 00 01 00` | **일부러 틀린 checksum** | 아무것도 안 움직여야 정상 |

- [ ] 3개 모두 기대대로 동작 확인

> 참고 — 개별 명령 프레임 만드는 법:
> `AA` + `모터ID` + `각도×10의 상위바이트` + `하위바이트` + `checksum(=ID+H+L의 하위 8bit)`
> 예: 90.0° → 900 = 0x0384 → H=03, L=84

---

## STEP 6. 각도 캘리브레이션

현재 초안값은 **OPEN=180° / GRIP=30°** (임의값). 실물에 맞게 조정:

1. [ ] 개별 명령(`AA 04 xx xx cs` 등)으로 손가락 하나씩 움직여보며 "예쁘게 쥐어지는 각도"와 "펴지는 각도"를 찾는다
2. [ ] `main.c` 상단 define 값을 찾은 값으로 수정:
   ```c
   #define FINGER_OPEN_ANGLE    180.0f   /* ← 찾은 펴기 각도로 변경 */
   #define FINGER_GRIP_ANGLE    30.0f    /* ← 찾은 쥐기 각도로 변경 */
   ```
3. [ ] 재빌드 → 플래시 → STEP 4 제스처 재확인

> ⚠ 손가락마다 각도가 달라야 하면 (예: 엄지만 다른 각도) 나한테 말할 것 — 채널별 각도 테이블로 확장 필요 (지금은 6개 동일 각도)

---

## STEP 7. 남은 검증 항목 (기능 동작 후)

- [ ] **FreeRTOSConfig.h를 나한테 업로드** → `configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY` 값 확인해서 USART1 우선순위(현재 6)가 안전한지 판정해줌
- [ ] 부팅 직후 초기 자세 결정: 지금은 CCR=0(펄스 없음)으로 시작 → 부팅 시 자동으로 OPEN 자세를 잡을지 결정해서 알려줄 것
- [ ] `uxTaskGetStackHighWaterMark`로 태스크 스택 여유 실측 (원하면 측정 코드 만들어줌)

---

## 문제 발생 시 보고 양식 (이대로 나한테 줄 것)

```
1. 어느 STEP에서 막혔는지:
2. 보낸 바이트:
3. 실제로 일어난 일 (어떤 서보가 어떻게 움직였는지 / 안 움직였는지):
4. 빌드 에러가 있다면 에러 메시지 전체:
```